apiVersion: v1
kind: Service
metadata:
  name: vite-app-service
  namespace: default
spec:
  selector:
    app: vite-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
